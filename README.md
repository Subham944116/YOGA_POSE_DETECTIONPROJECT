# YOGA_POSE_DETECTIONPROJECT
This is a minimal Django project that demonstrates a simple way to capture a camera snapshot in the browser and send it to a Django backend that uses MediaPipe to detect pose landmarks.
